## 1. ActivityStats の色同期ロジックの改善

- [x] 1.1 `src/views/ActivityStats.tsx` にチャート用の色を保持するステート（`chartColors`）を追加する。
- [x] 1.2 `isDark` の変更を検知し、DOMの更新を待ってからCSS変数を再取得する `useEffect` を実装する。
- [x] 1.3 `chartOptions` を `chartColors` ステートを参照するように修正し、React Compiler による自動最適化が効くように構成する。

## 2. 動作確認と検証

- [x] 2.1 ライトモードとダークモードを切り替えた際に、グラフのテキスト色（軸、タイトル、凡例）が即座に更新されることを確認する。
- [x] 2.2 グリッド線の色が背景に合わせて正しく切り替わることを確認する。
- [x] 2.3 カテゴリの色（バーの色）が引き続きテーマに合わせて正しく表示されていることを確認する。
