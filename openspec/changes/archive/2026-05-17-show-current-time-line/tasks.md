## 1. 準備とユーティリティの作成

- [x] 1.1 時刻計算用のユーティリティ関数を作成（スロット内の相対位置計算など）
- [x] 1.2 現在時刻を1分ごとに更新して取得するためのカスタムフック `useCurrentTime` の作成

## 2. コンポーネントの実装

- [x] 2.1 `TimeTracker` コンポーネントで `useCurrentTime` を使用し、現在表示中の日付が今日であるかを判定するロジックを追加
- [x] 2.2 `TimeSlotRow` コンポーネントに、現在時刻を受け取りインジケーターを表示するための Props とロジックを追加
- [x] 2.3 `TimeSlotRow` 内で現在時刻に応じた位置にインジケーター（横線）を絶対配置で描画する実装

## 3. スタイリングと仕上げ

- [x] 3.1 インジケーターの視覚的な調整（目立つ色、アニメーション、重なり順など）
- [x] 3.2 ダークモードおよびライトモードでの視認性の確認と調整

## 4. 検証

- [x] 4.1 「今日」の画面で現在時刻に応じた位置にラインが表示されることを確認
- [x] 4.2 「今日」以外の日付を表示した際にラインが表示されないことを確認
- [x] 4.3 時間の経過とともにラインが動くことを確認
